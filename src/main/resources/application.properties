# Spring Security - Queries for AuthenticationManagerBuilder
spring.queries.users-query=select email, password, active from users where email=?
spring.queries.roles-query=select u.email, r.role from users u inner join users_roles ur \
  on(u.id=ur.user_id) inner join role r on(r.id=ur.role_id) where u.email=? 
